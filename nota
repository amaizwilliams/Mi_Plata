

/* 
    function iniciarSesion(usuarioEntrada, passwordEntrada){
        let errorUsuarioEntrada=false
        do{
            for(let i=0; i<usuariosBancarios.length; i++){
        
    }
        let usuarioEntrada=prompt(`Usario: `)
        let passwordEntrada=prompt(`Contraseña: `)
        for(let i=0; i<usuariosBancarios.length; i++){
        if(usuarioEntrada===usuariosBancarios[i].nombreUsuario && passwordEntrada===usuariosBancarios[i].password && usuariosBancarios[i].estado===true){
            errorUsuarioEntrada=true;
            console.log("Sesion con exito");
            control()
            break;
        }
    }
        if(usuarioEntrada===usuariosBancarios.nombreUsuario && passwordEntrada!=usuariosBancarios.password){
            usuariosBancarios[i].intentos++
            actualizarStorage()
        }
        if(usuariosBancarios.intentos>1){
            console.log(`Al tercer intento tu cuenta sera bloqueda.\n
            llevas ${usuariosBancarios[i].intentos} intentos, te queda uno`);
        }
        if(usuariosBancarios[i].intentos>=3){
            usuariosBancarios[i].estado=false
            actualizarStorage()
            console.log(`Su Cuenta '${usuariosBancarios[i].nombreUsuario}'  ah sido bloqueda, por 24 horas, comunicarse con el banco`);
            return
        }
        if(usuarioEntrada==usuariosBancarios[i].nombreUsuario&&usuariosBancarios[i].estado==false){
            console.log("Su cuenta esta bloqueda");
            return
        }
        
        if(usuarioEntrada!=usuariosBancarios[i].nombreUsuario){
            console.log("El usuario no exite en el sistema");
            let salirSistema=parseInt(prompt("¿Desea salir del sistema? (1si/2no)"));
            if(salirSistema===1){
                return
            }
        }
        if(errorUsuarioEntrada===false){
            console.log("Credenciales incorrectas, volver a intentar ");
            
        }
    
        }while(errorUsuarioEntrada==false)
    } */